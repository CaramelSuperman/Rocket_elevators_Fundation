<%= form_with(model: intervention, local: true) do |form| %>
  <% if intervention.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(intervention.errors.count, "error") %> prohibited this intervention from being saved:</h2>

      <ul>
      <% intervention.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <%= form.select(:customer_id) do %>
                          <% @customer.each do |l| -%>
                           <%= content_tag(:option, l.Company_Name, value: l.id) %>
                       <% end %>
                    <% end %>
    <%= form.select(:battery_id) do %>
                          <% @customer.each do |l| -%>
                           <%= content_tag(:option, l.Company_Name, value: l.id) %>
                       <% end %>
                    <% end %>
    <%= form.select(:customer_id) do %>
                          <% @customer.each do |l| -%>
                           <%= content_tag(:option, l.Company_Name, value: l.id) %>
                       <% end %>
                    <% end %> 
      <%= form.select(:customer_id) do %>
                          <% @customer.each do |l| -%>
                           <%= content_tag(:option, l.Company_Name, value: l.id) %>
                       <% end %>
                    <% end %>
      <%= form.select(:customer_id) do %>
                          <% @customer.each do |l| -%>
                           <%= content_tag(:option, l.Company_Name, value: l.id) %>
                       <% end %>
                    <% end %>
      <%= form.select(:employee_id) do %>
                          <% @employee.each do |l| -%>
                           <%= content_tag(:option, l.first_name, value: l.id) %>
                       <% end %>
                    <% end %>                                                      
                    
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>


